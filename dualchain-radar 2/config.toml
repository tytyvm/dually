# Research hypotheses, NOT optimized or validated trading parameters.
chains = ["solana", "robinhood"]
strategy = "momentum" # use a separate database for pullback
initial_cash_per_chain = 1000.0
order_usd = 25.0
max_positions_per_chain = 3
max_market_cap = 500000.0
min_market_cap = 20000.0
allow_fdv_proxy = false # unknown market cap is rejected; opt-in uses FDV, clearly labeled
min_liquidity = 20000.0
min_age_s = 120
max_age_s = 86400
min_volume_m5 = 3000.0
min_buyers_m5 = 10
min_buy_fraction = 0.60
min_momentum_m5 = 0.10
max_momentum_m5 = 1.20
min_observations = 3
warmup_s = 60
max_gap_s = 180
latency_s = 2
pending_ttl_s = 180
cooldown_s = 1800
stop_loss = 0.20
take_profit_multiple = 2.0 # full exit baseline; test 3.0 and 5.0 on separate runs
trail_activation = 1.60
trail_drawdown = 0.20
max_hold_s = 1800
liquidity_exit_fraction = 0.50
daily_loss_fraction = 0.05
max_drawdown_fraction = 0.15
max_price_impact = 0.02
pool_fee_bps = 100.0 # deliberately pessimistic estimate, not a measured venue fee
extra_slippage_bps = 100.0
tx_fee_usd_solana = 0.10
tx_fee_usd_robinhood = 0.10
poll_seconds = 60
discovery_seconds = 300
watch_pools_per_chain = 15

